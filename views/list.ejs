<div id="heading" class="box">
    <h1>
        <%= todoDay %>
    </h1>
</div>
<%- include('header'); %>
    <div class="box">
        <% for (let i=0; i< todoList.length; i++) { %>
            <div class="item">
                <input type="checkbox">
                <p>
                    <%= todoList[i] %>
                </p>
            </div>
            <% } %>

                <form action="/" method="post" class="item">
                    <input type="text" placeholder="New Work" name="NewItem" autofocus autocomplete="off">
                    <button type="submit" name="ListType" value=<%=todoDay %>>+</button>
                </form>
    </div>
    <%- include('footer'); %>